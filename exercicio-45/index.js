const s = "leetcode"

function firstUniqueChar(s) {
const map = {}

    for (let i = 0; i < s.length; i++) {
        if (nums[i] in map) {
            return null
        }
        return nums[i]
    }
}